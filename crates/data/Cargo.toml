[package]
name = "data"
version = "0.1.0"
authors = ["Desmond Germans <desmond@germansmedia.nl>"]
edition = "2018"

[lib]
crate-type = ["staticlib"]

[dependencies]
rand = "*"
tokio = { version = "*", features = [ "full" ] }
codec = { path = "../codec" }
